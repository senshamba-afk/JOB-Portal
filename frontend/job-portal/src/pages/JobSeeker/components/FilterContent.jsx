import React from 'react'

const FilterContent = ({
    toggleSection,
    clearAllFilters,
    expandSections,
    filters,
    handleFilterChange,
}) => {
  return (
    <div>FilterContent</div>
  )
}

export default FilterContent